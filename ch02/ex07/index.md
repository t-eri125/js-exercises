## 問題 2.7
プログラムを実行し、挙動を確認しなさい。

### 回答
出力結果は以下のようになる。
```
0 1 0
1 1 0
```
以下のように解釈されていると考える。
``` ts
// 最初の出力の解釈
const c = a;    // a=0, c=0
++b;    // 	bが1増えて返ってくるので、b=1

// 次の出力の解釈
const e = a++;    // eにaが代入された後、aがインクリメントされるので、e=0, a=1
b;    // 	変化なしでb=1
```
