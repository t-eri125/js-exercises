// ゲッターメソッドは値の取得以外の処理も記述することができる。
//  一例として、以下のような値を読み出す度にその値が 1 ずつ増えていく
// ゲッターを持つクラスを作りなさい(初回呼び出しは0を返す)。

export class C {
    private count: number = 0;  // 初期値0

    get x() {
        return this.count++;    // 読みだすたびにインクリメント
    }
}
