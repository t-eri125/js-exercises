// オブジェクトが可変であることの確認
const obj1 = { x: 1 };  // ここはletの方がいい？
obj1.y = 2;             // 問題: ここに1行コードを書くことで以下の行で {x: 1, y: 2} が出力されること
console.log(obj1);

// オブジェクトが参照で比較されることの確認
const obj2 = { x: 1, y: 2 };
// 問題: 以下の行では何が出力されるか、予想してから結果を確認しなさい
// 〈予想〉false、obj2はobj1と別のオブジェクト（同じオブジェクトを参照していない）ため。
// 〈結果〉false
console.log(obj1 === obj2);

